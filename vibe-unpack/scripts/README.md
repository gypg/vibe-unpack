# 发布工具脚本

这个目录包含用于发布 vibe-unpack 的辅助工具。

## 目前可用脚本

（待实现）

建议未来可以添加：
- `generate-mermaid.js` - 从 demand-graph.json 自动生成 Mermaid
- `validate-schema.js` - 验证输出 JSON 是否符合 schema
- `package-release.js` - 打包发布版本
